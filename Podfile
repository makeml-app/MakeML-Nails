platform :ios, '9.0'

target 'Segmentation Nails' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DeepLabApp
  pod 'TensorFlowLiteGpuExperimental', '0.0.1'

end
